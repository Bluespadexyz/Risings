import cx from "classnames";

// ================================================

const EarnRisingsTicketsContainer = ({ children }) => {
  return <div className={cx("earn-loh-tickets-container")}>{children}</div>;
};

export default EarnRisingsTicketsContainer;
